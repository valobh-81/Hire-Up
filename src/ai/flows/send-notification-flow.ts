
'use server';
/**
 * @fileOverview A flow for sending notifications to students.
 *
 * - sendNotification - A function that handles the notification sending process.
 * - SendNotificationInput - The input type for the sendNotification function.
 */
// This file is kept for structure, but the logic is removed to make the app static.
